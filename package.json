{
  "name": "pkm-image",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "cleanup": "bash scripts/cleanup-dot-underscore.sh --quiet",
    "build": "npm run cleanup && vite build",
    "preview": "vite preview",
    "cleanup:deploy": "bash scripts/cleanup-and-deploy.sh",
    "cleanup:dry-run": "bash scripts/cleanup-dot-underscore.sh --dry-run"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.5.2",
    "vite": "^6.3.5"
  }
}
